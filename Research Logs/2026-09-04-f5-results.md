# F5 results: a real cost under chromatin ranking, a near-no-op at the head under score
ranking, and a benefit under neither

*(Title corrected 2026-09-08 — see Correction 2. As first published it read "a net cost,
dose-dependently, at every budget", which is true on `chromatin_od` and overstated on
`tm_score`.)*

Date: 2026-09-04. Run: `f5_nms_radius_ablation.py`, 2,203 s, 14 ROIs × 5 seeds × 3 radii ×
6 z × 2 axes = 5,040 arms, 45,360 rows. Pre-registered in
[`2026-09-04-f5-preregistration.md`](2026-09-04-f5-preregistration.md) (revision 3, converged
after two rounds of adversarial review, committed at `c2be222` **before** the sweep finished).

**All 3,741 checks passed.** Read `results/f5_nms_radius_ablation_verification.csv` before any
table below.

---

> ## Correction, 2026-09-04, same day — the arbiter sat on the wrong axis
>
> **`DECISIONS.md` D5 names this document.** F5 §3d declared `chromatin_od` primary because it is
> "the shipped ranker, commit `7c3af93`" — a claim inherited from F4 on the authority of a commit
> hash rather than a measurement. **Verified independently here and D5 is right:**
> `chromatin.rerank`'s only Python callers are the three superseded 2026-08-31 probes
> (`od_experiment.py`, `od_workload_ab.py`, `od_seed_sweep.py`); the two axis-sweep notebooks use
> it as an axis under comparison, not as a default; and `find_and_suppress.py` — the pipeline —
> contains no ranking step at all. Chromatin density was never the production ranker. F1, F4 and
> F5 each repeated the claim in turn.
>
> **What that does to §1.** The arbiter re-run on `tm_score`, the axis D5 says must arbitrate,
> from this same run's own CSV:
>
> | axis | `r5.9` mean paired Δ | `r5.0` | sign-flip p | ROIs with any change |
> |---|---:|---:|---:|---:|
> | `chromatin_od` (as published below) | −0.0148 | −0.0223 | 0.00098 | 11 / 14 |
> | **`tm_score` (the arbiter D5 requires)** | **−0.0018** | **−0.0019** | **0.0625** | **5 / 14** |
>
> The direction is unchanged — negative at 16 of 18 budget levels on `tm_score` too — but the
> magnitude is ~12× smaller and **does not clear 0.05**. p = 0.0625 is the attainable floor when
> only 5 of 14 clusters move at all. **§1's "significantly worse" holds on `chromatin_od` and does
> not hold on `tm_score`.**
>
> **The gap is itself the most interesting thing in this run, and it has a mechanism.** On
> `tm_score`, pooled Δ(recall@K) is ≈0.0000 at every K from 10 to 200, and 9 of 14 ROIs have
> *exactly zero* Δ at K = 250. That is not noise — it is structural. NMS sorts by the match score,
> so the highest-scoring peaks survive at **any** radius and the head of a score-ranked list is
> close to invariant to this knob. The radius only reshuffles lower-scoring material, which
> matters when an *independent* key (chromatin density) can promote that material — a surviving
> duplicate on an already-found nucleus — into the top 250. So the entire §1 effect is the cost of
> re-ranking a longer list by a key that does not know what NMS was doing.
>
> **What survives unchanged.** The §3 false-positive answers: the candidate-count ratio (+23.5 % /
> +37.3 %) is a property of the candidate *set* and does not depend on the ranking axis at all, and
> the full-list and duplicate accounting move with it. **"Shrinking gives fewer false positives" is
> still false on every framing, on either axis.** §5's coverage guard and §6's mis-centring finding
> are also axis-independent.
>
> **What the corrected bottom line is.** Under the ranker that ships, shrinking the NMS radius is
> approximately a **no-op at the head and a mild cost deeper in the list**, not the decisive loss
> §1 reports. It is still not a benefit on any measured axis, and it still costs 23–37 % more
> candidates, so §7's recommendation (padding ON, radius back to the 7.5 µm default) is unchanged
> — but it now rests on "no measured benefit at real cost" rather than on a significant recall
> loss.
>
> Everything below is left as published, per this repo's amend-rather-than-edit rule. Read every
> "arbiter" and "significant" claim in §1 as scoped to `chromatin_od`.

> ## Correction 2, 2026-09-08 — the retraction above was itself wrong, and eight further defects
>
> An independent audit of this document, and then of the correction list itself, found errors in
> both the published results **and** in Correction 1. Every number below was recomputed from the
> committed CSVs. **This block supersedes Correction 1's conclusion and the sentences named in
> §12; everything else below stands as published.**
>
> ### 2.1 Correction 1 demoted `chromatin_od` without running the test that decides it
>
> Correction 1 accepted an argument that `chromatin_od` must not arbitrate, and rescoped the
> headline to "no measured benefit at real cost". **That retreat was wrong.** F5 computes both
> axes, so the head-to-head was already in the CSV and was never looked at. Run on the control
> arm at the headline cell (recall@250, z = 1.0, mean-of-5 paired Δ per ROI, 14 clusters, exact
> 2¹⁴ sign-flip, cluster bootstrap `seed=20260904`):
>
> ```
> chromatin_od − tm_score:  mean +0.0869   CI [+0.0419, +0.1332]   p = 0.0020   12/14 ROIs
> ```
>
> It is not fragile. Both shrink arms agree (`r5.9` +0.0738, p = 0.0088; `r5.0` +0.0665,
> p = 0.0190, 9/14 each); all six z levels give +0.086 … +0.096 with p ≤ 0.0022 and 12/14; and it
> is positive with p < 0.05 at every K from 25 to 5,000 (K = 10 p = 0.23, K = 10,000 ≈ 0).
>
> **So §1's `chromatin_od` numbers are valid and are restored**, explicitly scoped to that axis.
> Both axes are now reported with **neither declared secondary** — see §2.6 for why this is not a
> promotion of the other direction either.
>
> ### 2.2 What is true about the ranker, carried first-hand
>
> The factual half of the argument Correction 1 accepted is correct, and is restated here as F5's
> own finding, verified at `HEAD` so it does not depend on any uncommitted file:
>
> * `git grep "\.rerank(" HEAD` returns exactly `od_experiment.py:131`, `od_seed_sweep.py:118`,
>   `od_workload_ab.py:80` — all three superseded 2026-08-31 probes.
> * `find_and_suppress.py:148-149` runs `nms_by_distance` and keeps `centers[keep]`; line 174
>   emits `rank = np.arange(len(centers))` over that keep order, which `nms.py:44` builds by
>   `argsort(-scores)`. There is no chromatin path in the pipeline.
>
> **Chromatin density was never the production ranker.** This document's §3d claim that it is
> "the shipped ranker, commit `7c3af93`" was inherited from F4 on the authority of a commit hash
> rather than a measurement, and is withdrawn. (An uncommitted `DECISIONS.md` D5, written by
> another session, reaches the same conclusion independently; it is **not** cited as the authority
> here, because it and the `verify_chromatin_ranker.py` it names are absent from the repository —
> see §2.8.)
>
> ### 2.3 §6's mis-centring finding is refuted by its own ledger — retracted
>
> Retracted: *"On canine lung cancer (10.9–15.6 px) and canine soft tissue sarcoma (16.9–18.7 px)
> the whole band sits below even `r5.0` … every arm suppresses the entire band"*, and the last
> sentence of §6, *"any future attempt should size the radius per domain from this table."*
> **§7's configuration recommendation — padding ON, radius back to 7.5 µm — is untouched and
> stands.**
>
> The table is a **mean over cells of per-cell percentiles**, it silently drops the 13 cells with
> `gap_n == 0`, and cells with `gap_n == 1` have p05 = median = p95. At the control radius:
>
> ```
> canine soft tissue sarcoma  11 gaps, 7 of 10 cells, 3 cells with n=1
>                             459/s0 = 27.07 px  and  460/s3 = 20.62 px  are ABOVE r5.0 = 20.15
> canine lung cancer           4 gaps from 2 of 10 cells
> ```
>
> Two structural caveats belong with any future use of this quantity: `own` is the nearest
> deep-pool peak from an **uncapped** `k=1` query, so an annotation with no peak on it still
> contributes a gap; and gaps are bounded above by the radius **by construction**, so the band
> narrowing across the dose (p95 26.4 → 20.1 → 17.5 px) is mechanical truncation, not a finding.
> The column headed `n` in §6's table is a **mean gap count per cell**, not a sample size.
>
> ### 2.4 "v2's 23–29 px was near the top of the distribution" — retracted, it is typical
>
> The row labelled "pooled" is a mean of per-cell percentiles, not a pooled percentile, and is
> systematically narrower than any real distribution. Per-cell medians at the control radius:
> n = 57 non-NaN, min 10.8, IQR 18.4–26.7, **median 23.9**, max 31.2; 28 of 57 (49 %) inside
> 23–29. Weighting by gap count, **483 of the 689 gaps (70.1 %)** sit in cells whose median is
> inside 23–29, and the weighted mean of medians is 23.06. That per-cell-median view is *itself*
> unpooled — it weights a 1-gap cell like a 29-gap cell — and the true per-gap distribution is
> **not recoverable from the committed CSVs**, which store only `gap_n` and three percentiles.
> The supportable statement is that v2's placement was **under-evidenced** (one ROI, one seed) and
> the true spread is wide, not that its band sat high.
>
> ### 2.5 Claims that were wrong, unsupported, or mis-scoped
>
> | § | published | correct |
> |---|---|---|
> | §1, §4, §7 | "a quarter to a third of the top-250 changes identity" | `topk_churn` is Jaccard **distance**; with \|A\|=\|B\|=250 that is **14.2 % / 19.1 %** |
> | §1 | "It is an active intervention, not an inert one" | True on `chromatin_od`. On `tm_score` churn is 0.0141 / 0.0210 → **0.7 % / 1.1 %**, which is the **inert** case by §6.6's own criterion. At K = 250 on `tm_score`: `r5.0` 58 tie / 1 better / 11 worse, `r5.9` 59 / 1 / 10, **0 strictly dose-monotone**. This *supports* §2.7's mechanism rather than undermining it |
> | §1 | 5,435–5,682 "ground-truth claim reassignments" | `evaluate.py:81` sets `matched_rank` to a **positional index** in the ranked list, which shifts on any longer re-sorted list. Withdrawn as evidence. It also never implemented the registered quantity — pre-registration §6.6 asked for a change of rank **bucket** |
> | §1 | cell Wilcoxon "< 1e-12" | **5.32e-10** (`r5.9`) and **2.45e-11** (`r5.0`); on `tm_score`, **0.062 / 0.040** — never reported, and it disagrees with the `tm_score` arbiter. `zsplit` is conservative relative to **Pratt**, not to dropping zeros (5.32e-10 vs 1.11e-8), so the anti-conservatism comes only from treating 70 clustered cells as independent — a heuristic expectation under positive intra-cluster correlation, not a provable bound |
> | §1 | "monotone in 95.7 % of the 70 cells" | 67/70 includes **20 cells where all three arms are exactly equal**. Correct: **47 of the 50 cells where the radius changed anything (94 %)**, strictly ordered in **20** |
> | §1 | "Not one cell in 70 has `r5.0` beating the control" | `chromatin_od`-scoped (50 worse / 20 tie / 0 better). On `tm_score`, 246.tiff seed 2 beats the control |
> | §3 | `FP_to_r` and `read_*` | `chromatin_od`-only and unlabelled. On `tm_score`, `FP_to_0.9` is 2,541 → 2,600 → **2,714 (+6.8 %)** against the published +30 %, and `read_50` is 290 → 300 → 309. **Direction survives on both axes.** The candidate-count ratio (1.2350 / 1.3733) and `dup_fp_frac` (0.00274 / 0.00431 / 0.00472) are genuinely axis-free — verified identical on both |
> | §1 | "no crossover K\* worth the name" | That is the **pooled mean**, a different quantity from the pre-registered per-cell K\*. On `chromatin_od`, **22/70** (`r5.0`) and **29/70** (`r5.9`) cells do cross, medians 350 and 500; per domain (`r5.0`) 25 / 25 / 212.5. On `tm_score`, **15/70** and **12/70**, medians **3,000 / 4,000** — that is the number that actually retires P3 for the product |
> | §4 | "24 clean gains, 0 losses, dose-monotone" | Gains are **24 at both doses**, per-domain split identical. There is no dose response in the ledger; the tail gain saturates at `r5.9`, exactly as pre-registration §2 predicted. Monotonicity is a property of recall@250, not of this ledger |
> | §8 | the three exact-zero clusters are "ceiling-adjacent" | Only **013** is a ceiling (recall 1.0 on all 15 cells). 201 and 529 are **metric granularity**: with 17 and 19 mitoses one TP is 0.059 / 0.053, and 233 also has 17 and *does* move (−0.0353). The "if anything, understated" conclusion survives on the granularity reading; informative-cluster means are **−0.0188 / −0.0284** — an adjustment to the *estimate* only, since the 1/14 weight cancels in the permutation |
> | header | "5,040 arms" | **2,520** (45,360 rows ÷ 18 budgets) |
> | §7 | "`check_min_separation` … (3,570 checks)" | **210**. 3,570 was three checks summed; the 3,741 total is right |
> | §7 | "336 cells × 7 ROIs" | 336 **rows** across 7 ROIs |
> | §3 | "3 of 3,780 cells" | rows; and the guard bites at K = 3,000 at z = 3.0, not only at K ≥ 5,000 as §3d predicted |
> | Corr. 1 | `tm_score` `r5.9` Δ "−0.0018"; "negative at 16 of 18 budget levels" | **−0.0017**; **15/18** (`r5.9`) and **17/18** (`r5.0`) |
> | §4 | full-list table | is z = 1.0; the label was missing |
>
> ### 2.6 Multiplicity, and why the arbiter is not being re-declared
>
> **Holm across the 2 tests on `chromatin_od` (0.00195 / 0.00195) is the arbiter the run actually
> executed** — revision 3's registered protocol, and `PRIMARY_AXIS = "chromatin_od"` in the code
> at all three commits. Holm across all 4 tests gives **[0.0039, 0.0039, 0.125, 0.125]** and is
> reported as a **declared post-hoc widening**. Neither replaces the other, and no conclusion
> moves between them.
>
> `chromatin_od` is deliberately **not** re-declared primary. Pre-registration §8, unchanged
> through every revision: *"'Holm across 4' and 'the arbiter is chromatin_od' cannot both stand"* —
> a sentence written to close exactly this forking path. §2.1's head-to-head was run **after**
> seeing both axes' answers, so using it to select the axis that produces significance is the move
> that rule forbids. Both axes reported, neither secondary.
>
> **And this is not a promotion of `od51`.** F5 does not test `od31`, `od_falloff` or
> `mask_od_mean` at all and takes no position on the chromatin family. The one number that looks
> like it touches that question is descriptive only: paired Δ`read_95` = **−461 px, CI [−1043,
> +172], p = 0.17**, shorter on 10 of 14 — favourable, statistically silent.
>
> ### 2.7 The mechanism, which the corrected numbers support
>
> `chromatin_od` churn 0.249/0.321 against `tm_score` 0.014/0.021 is not noise. **NMS sorts by the
> match score**, so the highest-scoring peaks survive at any radius and the head of a score-ranked
> list is close to invariant to this knob. The radius reshuffles lower-scoring material, which
> matters only when an *independent* key can promote a surviving duplicate on an already-found
> nucleus into the top 250. On `tm_score`, 9 of 14 ROIs have exactly zero Δ — including 548 (238
> mitoses) and 300 (180), where granularity cannot be the explanation. Their flatness is the
> direct evidence for this mechanism.
>
> ### 2.8 Provenance — two process failures, both mine to disclose
>
> **(a) I staged a file I had not re-read.** Commit `49d10ca`'s message describes the only
> pre-registration change as §10's cost block. It was not: that commit also swept in another
> session's uncommitted **revision 4**, which moved the arbiter from `chromatin_od` to `tm_score`.
> In that same commit this document says *"Arbiter (pre-registered §8) … `chromatin_od`"*, citing
> a section that by then said `tm_score`. Revision 4 also rewrote §3d and §8 **in place** rather
> than as a dated append — for the one edit that changes what was pre-registered — while §10's
> cost correction did use a dated block. Correction 1 therefore describes a correction to a §3d
> that no longer said what it claims. **The executed configuration was `chromatin_od` throughout:
> `PRIMARY_AXIS` is unchanged at `c2be222`, `49d10ca` and `e610e6b`.**
>
> **(b) An undisclosed protocol deviation.** Pre-registration §6.5 (line 349) registered that
> *"the win/loss half stays on both axes"*, and §11 row 9 repeated it. The code gated it on
> `axis == PRIMARY_AXIS`, so `gained_vs_control`, `lost_vs_control` and `gt_claim_change` are
> **−1 on all 210 `tm_score` ledger rows** and are unrecoverable for that axis without a re-run.
> The code is fixed to compute both axes; the committed CSVs are **not** regenerated, so code and
> data deliberately diverge from here. This is acceptable only because `gt_claim_change` is
> withdrawn (§2.5) and "0 lost" is corroborated by an independent path — 0 of 70 cells have a dose
> arm's `full_list_recall` below the control's.
>
> **(c) Absent authority.** Correction 1 opened with *"`DECISIONS.md` D5 names this document"*.
> `git show HEAD:DECISIONS.md` contains D1–D4 only and `verify_chromatin_ranker.py` is untracked,
> so that correction cited an artifact no reader of this repository can find — the same failure
> mode as (a), one level up. §2.2 carries the evidence first-hand instead.
>
> ### 2.9 The corrected bottom line
>
> Shrinking the NMS radius is **a real cost at the operating point under chromatin ranking**
> (−0.0148 / −0.0223 recall@250, Holm p = 0.00195), **a near-no-op at the head and a mild cost
> deeper under score ranking** (Δ ≈ 0.0000 at K = 10–200, negative at 15/18 and 17/18 budget
> levels, reaching −0.0054 at K = 5,000), and **a tail gain on both axes that is the wrong trade
> at K = 250** (24 gained, 0 lost; worst full recall 0.9663 → 0.9923, with coverage rising
> 0.837 → 0.879). It costs 23.5 % / 37.3 % more candidates on every axis. **§7's recommendation is
> unchanged: padding ON, NMS radius back to the 7.5 µm default.**


## 1. The headline

Holding border padding ON in every arm and moving only the NMS radius, **shrinking it makes
retrieval worse** — at every budget from K = 10 to K = 10,000, monotonically in the dose, and
significantly at the ROI-cluster level.

**Arbiter** (pre-registered §8): mean-of-5 **paired** Δ(recall@250) per ROI, z = 1.0,
`chromatin_od`, 14 clusters, exact 2¹⁴ sign-flip + cluster bootstrap, Holm across 2.

| dose | mean paired Δ | 95 % cluster-bootstrap CI | sign-flip p | Holm p | cell Wilcoxon (anti-conservative) |
|---|---:|---|---:|---:|---:|
| `r5.9` (23.3–26.1 px) | **−0.0148** | [−0.0206, −0.0091] | 0.00098 | 0.00195 | < 1e-12 |
| `r5.0` (19.7–22.1 px) | **−0.0223** | [−0.0316, −0.0135] | 0.00098 | 0.00195 | < 1e-12 |

Both CIs exclude zero. The sign-flip p is at its attainable floor: 11 of the 14 ROIs have a
strictly negative mean Δ and 3 are exactly 0.0000, so the effective enumeration is 2/2¹¹.

**Not one cell in 70 has `r5.0` beating the control at K = 250** — 71.4 % are strictly worse and
the remainder tie.

### The registered falsifier, and which way it fell

With P4 withdrawn before the run (§2 below), the surviving mechanism test was **monotonicity
across the dose**: if the effect is the same-object secondary peak v2 traced, `r5.9` must sit
between `r7.5` and `r5.0`. It does, in **95.7 %** of the 70 cells. This is an ordered effect of
suppression, not noise that happened to land negative.

### It is an active intervention, not an inert one

`topk_churn` against the control is **0.249** (`r5.9`) and **0.321** (`r5.0`) — a quarter to a
third of the top-250 list changes identity, with 5,435–5,682 ground-truth claim reassignments.
This is the column F4 §6 built an entire pre-flight to obtain, and it is what makes the null
interpretable: the radius genuinely moves the list, and the list it moves to is worse.

---

## 2. What was withdrawn before the run, and why it matters to reading this

Revision 1 justified the two doses as a **mechanism decomposition** — 5.9 µm to stop merging
distinct annotations, 5.0 µm to additionally stop the same-object secondary peak. Measured over
all 14 ROIs, that is not available:

| radius | mitotic↔mitotic pairs inside | any↔any pairs inside |
|---|---:|---:|
| 7.5 µm (control) | **1** (245.tiff) | **7** (1 on 245, 6 on 403) |
| 5.9 µm | 0 | 0 |
| 5.0 µm | 0 | 0 |

**Zero** pairs lie between the two doses, so they are identical with respect to un-merging ground
truth, and across ~1,325 mitotic annotations exactly one pair is close enough for the *default*
radius to merge at all. The ladder was re-registered as a one-mechanism dose–response and P4
withdrawn — before any result existed, which is the only time a withdrawal is worth anything.

This also **retracts an overstatement revision 1 made about F4**. F4's claim ("shrinking cannot
recover a merged pair of ground-truth mitoses") is about the 5.0 µm radius, and 6.594 µm > 5.0 µm
holds on the 14-ROI set. F4 was right; revision 1 was not.

---

## 3. The three false-positive questions, kept apart

The request was "does our candidate list have fewer FPs overall". That is three questions, and
only the second and third are independent of §1.

**(1) At fixed K — the same question as §1.** `FP@K = K − TP@K`, so precision@K is an affine
restatement of recall@K. Reported, labelled as such, not offered as corroboration. (The
`budget_delivered == budget` guard fired on 3 of 3,780 cells, none at the headline.)

**(2) Over the full list — worse, and necessarily so.**

| domain | `r7.5` | `r5.9` | `r5.0` |
|---|---:|---:|---:|
| pooled ratio to control | 1.000 | **1.235** | **1.373** |
| canine lymphosarcoma | 16,879 | 24,872 | 31,760 |
| canine soft tissue sarcoma | 12,441 | 13,808 | 14,396 |

A shrink suppresses less, so more candidates survive; this was known before the run and is
reported in one line rather than dressed up. The domain spread is large — lymphosarcoma nearly
doubles, soft-tissue sarcoma grows 16 %.

**(3) FPs paid to reach a recall target — worse at every target.**

| target | `r7.5` | `r5.9` | `r5.0` | coverage-guarded? |
|---|---:|---:|---:|---|
| 0.80 | 632 | 725 | 783 | no |
| 0.90 | 1,786 | 2,034 | 2,326 | no |
| 0.95 | 3,544 | 4,100 | 4,559 | no |
| 0.99 | 5,753 | 7,352 | 8,157 | **yes — see §5** |
| 1.00 | 5,871 | 8,243 | 9,102 | **yes — see §5** |

The two guarded rows are the ones the pre-registration refuses to let carry a claim. The unguarded
rows say the same thing anyway.

**Duplicate detections on already-claimed objects**, reported with the denominator that makes them
comparable across arms of different length: `dup_fp_frac` rises **0.0027 → 0.0043 → 0.0047**. This
is precisely the defect `invariants.check_nms_radius` exists to prevent, reappearing
dose-dependently — the invariant was right.

Reading depth moves the same way (`read_50` 128 → 142 → 156; `read_95` 3,634 → 4,189 → 4,648),
which reproduces the direction of the 2-ROI sweep in
[`2026-09-01-tm-variant-sweep.md`](2026-09-01-tm-variant-sweep.md) §5f on a balanced sample.
`read_50` is a diagnostic here, never an arbiter (D4 §5).

---

## 4. What the shrink *does* buy, stated so it is not lost

The tail improves, and the improvement is real:

| | mean full-list recall | worst-of-70 |
|---|---:|---:|
| `r7.5` | 0.99745 | 0.96629 |
| `r5.9` / `r5.0` | 0.99982 | 0.99231 |

And the **win/loss ledger is clean**: **24 gained, 0 lost**, at both doses, across 70 cells and
6,555 annotation-instances, with gains concentrated in 13 of the 70 cells. Greedy NMS's
non-monotonicity in the radius — real, and the reason nesting was deliberately not asserted —
never actually bit on this data. So the shrink does not shuffle; it adds.

**But it is a bad trade at the operating point.** ~24 annotation-instances recovered, for 23–37 %
more candidates, a quarter-to-a-third of the top-250 list churned, and −0.015 to −0.022 recall at
K = 250. And §5 shows part of the tail gain is not evidence at all.

---

## 5. Coverage — why the tail gain is not the headline it looks like

| z | `r7.5` | `r5.9` | `r5.0` |
|---|---:|---:|---:|
| 0.5 | 0.894 | 0.928 | 0.935 |
| **1.0** | **0.837** | **0.871** | **0.879** |
| 2.0 | 0.675 | 0.703 | 0.711 |
| 3.0 | 0.396 | 0.406 | 0.410 |

A shrink keeps more peaks → raises coverage → mechanically raises full-list recall. Coverage rises
with the dose at every z, so the §4 tail gain arrives partly by geometry. This is why
`full_list_recall` was pre-registered as never-a-headline, and why the same guard was extended to
`read_99` / `read_100` / `FP_to_1.0` — otherwise the saturated regime is exactly where a shrink
would have looked like it won.

---

## 6. The dose grid was mis-centred on two domains — the check that caught it

The 23–29 px same-object suppression band carried the entire justification for where 5.9 and 5.0
sit, and it came from **one ROI and one seed** (v2 cells 9–11). Re-measured on all 14 ROIs × 5
seeds, at the control radius:

| domain | gap p05 | median | p95 | n |
|---|---:|---:|---:|---:|
| canine cutaneous mast cell tumor | 11.9 | 22.7 | 28.4 | 26.8 |
| canine lymphosarcoma | 13.9 | 22.9 | 28.7 | 28.8 |
| human neuroendocrine tumor | 20.4 | 25.9 | 29.9 | 6.0 |
| human melanoma | 17.3 | 24.6 | 26.3 | 2.9 |
| human breast cancer | 18.4 | 22.1 | 26.4 | 2.9 |
| **canine soft tissue sarcoma** | **16.9** | **17.8** | **18.7** | 1.1 |
| **canine lung cancer** | **10.9** | **12.1** | **15.6** | 0.4 |
| pooled | 16.2 | 22.5 | 26.4 | — |

v2's 23–29 px was near the **top** of the true distribution, not typical of it. On canine lung
cancer (10.9–15.6 px) and canine soft tissue sarcoma (16.9–18.7 px) the whole band sits *below*
even `r5.0`'s 19.7–22.1 px, so on those two domains no dose in this ladder addresses the mechanism
at all — every arm suppresses the entire band.

This does not rescue the negative result: those are also two of the domains where the shrink
costs least, and the effect is negative in 11 of 14 ROIs regardless. But it means **a
correctly-centred ladder for those domains was never tested**, and any future attempt should size
the radius per domain from this table rather than from 301.tiff.

---

## 7. What this means for the pipeline

**v2's recall improvement came from the border padding, not the radius.** The two were changed
together and reported together; separated, padding is the part that helps and the radius shrink is
a net cost that padding's gain masked. The defensible configuration is **padding ON, NMS radius
back at the 7.5 µm default** — which also restores `invariants.check_nms_radius` rather than
overriding it.

The named mechanism v2 traced on 301.tiff is real (24 clean gains, 0 losses, dose-monotone), but
it is worth about 24 annotation-instances in 6,555 and costs 23–37 % more candidates. At the
operating point the product actually uses, that is the wrong trade.

**Verification that makes the above trustworthy rather than merely computed:**

* `r5.0` at seed 0 reproduced `results/tm_ccoeff_threshold_axis_sweep_v2.csv` to
  `max_abs_recall_diff = 1.1e-16` and `max_abs_ndet_diff = 0` over 336 cells × 7 ROIs on **both**
  axes — the whole pipeline validated against an independent earlier implementation.
* `check_min_separation` on every arm at its own radius (3,570 checks); `check_nms_radius` on the
  control as a positive control; the one-match-many-z shortcut at z = 1.0 **and z = 3.0** for all
  three radii (84 checks); `od` hoist equivalence; `valid.all()`, `od_nan == 0` and
  `n_gt_within_seed_hole == 0` asserted per cell.

## 8. Limitations

* **One template configuration** (single 51 px, `TM_CCOEFF`, `hematoxylin_od`). The same-object
  gap is a property of the correlation surface, hence of the template, so §6's table does not
  transfer to a tightened template — that is F4's question.
* **n = 14 clusters** bounds resolution: the smallest attainable two-sided sign-flip p is 2/2¹⁴,
  and with 3 exact zeros the realised floor here was 2/2¹¹.
* **recall@250 is ceiling-adjacent** on the four ROIs carrying 17–19 evaluation mitoses
  (201, 233, 013, 529); three of the four contribute the exact-zero Δs, diluting the pooled effect
  toward zero. The effect is therefore, if anything, understated.
* **The cost estimate was wrong.** §10 predicted ~9–12 min; the run took **37 min**. The estimate
  came from a single `246.tiff` probe and under-counted the per-arm evaluation work (three arms ×
  six z × two axes of `bucket_detections`, plus cached `coverage_fraction`). Budget ~2.5 min per
  ROI for a re-run of this shape.
